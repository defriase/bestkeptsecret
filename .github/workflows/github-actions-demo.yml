---
name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing this shit
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: "This is an echo of a secret!"
        with:
          yt_api_df: $ {{ secrets.YT_API_DF }}
        run: echo "${yt_api_df}"

